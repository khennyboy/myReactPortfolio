import Header from "../ui/Header";
import Nav from "../ui/Navs";

const Profile = () => (
  <header className="relative h-svh pb-8">
    <Nav />
    <Header />
  </header>
);

export default Profile;
